{
  "name": "@digitaplatform/digita",
  "version": "0.1.1",
  "type": "module",
  "description": "Digita — the platform's own FREE signature (the default identity): cool navy/cyan brand world, 40px grid + top glow, cloud mark + wordmark. Reproduces digitacloud.app. Composes on top of any design; ships as pure config (no runtime CSS).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./digita-plugin.json": "./dist/digita-plugin.json"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/digitaplatform/digita-plugins-free.git",
    "directory": "digita"
  },
  "digita": {
    "id": "digita",
    "type": "signature",
    "tier": "free",
    "sdk": "^0.1.0",
    "displayName": "Digita"
  },
  "scripts": {
    "build": "tsc && gen-signature",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@digitaplatform/signature-build": "workspace:*",
    "@digitaplatform/theme": "^0.1.0",
    "typescript": "^6.0.3"
  }
}
